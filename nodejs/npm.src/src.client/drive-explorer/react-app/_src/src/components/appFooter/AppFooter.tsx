import React from "react";

import "./AppFooter.scss";
import { AppFooterProps } from "./AppFooterProps";

const AppFooter = (props: AppFooterProps) => {
  return <footer className="trmrk-app-footer"></footer>;
};

export default AppFooter;
